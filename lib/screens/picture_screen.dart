import 'package:flutter/material.dart';
import 'package:interns_task/widgets/posts.dart';

class PostsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Posts(),
    );
  }
}
